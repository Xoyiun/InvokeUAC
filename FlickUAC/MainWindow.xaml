<Window x:Class="FlickUAC.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="FlickUAC" Height="500" Width="500">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,10">
            <ComboBox x:Name="LanguageMenu" HorizontalAlignment="Left" Width="120" VerticalAlignment="Center" SelectionChanged="LanguageMenu_SelectionChanged"/>
            <Button x:Name="ReFlash" Content="ReFlash" HorizontalAlignment="Right" VerticalAlignment="Center" Click="ReFlash_Click"/>
        </Grid>

        <DataGrid x:Name="ItemList" 
          Grid.Row="1"
          AutoGenerateColumns="False" 
          IsReadOnly="True"
          HeadersVisibility="None"
          SelectionMode="Extended" SelectedCellsChanged="ItemList_SelectedCellsChanged">

            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                </Style>
            </DataGrid.RowStyle>

            <DataGrid.Columns>
                <DataGridTemplateColumn Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Image Grid.Column="1" Source="{Binding Icon}" Margin="5,5,5,5"/>
                                <TextBlock Grid.Column="2" Text="{Binding ItemPath}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" TextWrapping="NoWrap" ToolTip="{Binding ItemPath}"/>
                            </Grid>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="2" Margin="0,10,0,0">
            <Button x:Name="ItemAdd" Content="Add" HorizontalAlignment="Left" VerticalAlignment="Center" Click="ItemAdd_Click"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Button x:Name="ItemLocation" Content="Location" Margin="0,0,8,0" Click="ItemLocation_Click"/>
                <Button x:Name="ItemDelete" Content="Delete" Click="ItemDelete_Click"/>
            </StackPanel>
        </Grid>
        <Grid/>
    </Grid>
</Window>