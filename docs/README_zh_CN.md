**[中文繁體](README_zh_TW.md) | [中文简体](README_zh_CN.md) | [English(US)](README_en_US.md)**
# InvokeUAC
通过在注册表中加入 RunAsInvoker 兼容性层级，强制让特定文件以呼叫者权限执行，从而避开用户帐户控制 UAC (User Account Control) 的权限提升请求。

## 核心功能

### 1. 现代化清单管理
* **资源自动解析**：程序会自动解析并提取路径对应的可执行文件图标，提供直观的视觉标识，方便快速辨识项目。
* **响应式选取逻辑**：重构后的选取机制确保在大规模数据下仍能保持流畅的操作反馈。
* **高性能架构**：基于 .NET 10.0 开发，确保在处理系统底层注册表时具备极高的执行效率。

### 2. 进阶批量操作
* **多重项目选取**：支持标准的 Extended Selection 模式，可通过鼠标拖拽、Shift 或 Ctrl 键同时选取多个目标项目。
* **同步处理能力**：支持针对多组注册表键值进行同步删除或文件位置检索。

---

## 功能按钮操作说明

### 1. 加入项目
* **作用**：手动建立新的 UAC 绕过注册表项。
* **用法**：点击“加入”按钮，在弹出的对话框中选取欲执行之 `.exe` 文件或输入特定指令路径。确认后程序将在对应的注册表路径下建立对应的注册表值。

### 2. 批量删除
* **作用**：从系统注册表中移除选定项目的绕过设定。
* **用法**：在清单中选取一个或多个项目后点击“删除”。程序将调用注册表 API 执行删除作业，并同步将其从 UI 清单中移除。

### 3. 批量查看详细内容
* **作用**：检视选定项目的详细路径、图标信息以及定位实体文件。
* **用法**：
    * **内容预览**：鼠标停留在项目上会显示完整路径。
    * **文件定位**：选取项目后点击“位置”，程序将调用文件资源管理器，自动打开文件夹并选中该实体文件。

### 4. 语言切换
* **作用**：切换界面显示语言。
* **用法**：点击语言切换按钮。程序将即时从 JSON 语系文件重新映射界面字符串，实现 Runtime 状态下的界面语系更换，无需重新启动程序。

### 5. 重新整理
* **作用**：重新扫描注册表并同步最新的数据状态。
* **用法**：点击“重新整理”按钮。程序会重新遍历目标注册表路径并重新加载数据，确保 UI 显示与系统实际状态完全一致。

---

## 技术规格
* 执行平台：Windows x64
* 目标框架：.NET 10.0
* 用户界面：WPF (Windows Presentation Foundation)

## 部署与下载说明

为确保程序能正常启动，请根据您的系统环境选择对应的版本：

### 独立版 - 推荐
* **适用对象**：一般用户，或不确定电脑是否安装 .NET 10 环境的系统。
* **说明**：此版本采用 Self-contained 部署，已包含 .NET 10 必要组件，文件体积较大但具备最佳兼容性。

### 标准版
* **适用对象**：已手动安装 .NET 10 Runtime 的用户。
* **说明**：文件体积极小，依赖系统已存在的 .NET 环境，若环境版本不符则无法启动。

## 操作安全性提示
本工具涉及对系统注册表 `HKEY_CURRENT_USER` 路径的写入与删除操作。在执行“批量删除”或“加入项目”前，请务必确认操作目标符合您的预期。

# 一切歧异以繁体中文为准
